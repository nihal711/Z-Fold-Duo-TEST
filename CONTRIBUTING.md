# Contributing

## 開発環境

- JDK 17
- Android SDK 37
- Galaxy Z Fold7またはFold8の実機

変更前にIssueで目的と実機での再現条件を共有してください。Display状態制御は端末固有のため、エミュレーターだけで完了とはみなしません。

## 検証

```bash
./gradlew testDebugUnitTest lintDebug assembleDebug
```

表示制御を変更した場合は、実機で次を確認してください。

1. 閉じた状態から全開までゆっくり開く
2. 全開から閉じる
3. 中間角度で1秒以上停止する
4. 開閉を3回以上繰り返す
5. 閉じた状態で電源ボタンから外画面を再点灯する

検証後、`adb logcat -s ZFoldDuoEngine`にクラッシュ、残留Window、画面状態の反復がないことを確認してください。

## Pull request

- 変更は一つの目的に絞ってください。
- 新しい計算ロジックには単体テストを追加してください。
- 端末固有の識別子、署名鍵、ペアリング鍵、画面キャプチャをコミットしないでください。
- 外部コードを取り込む場合はライセンス互換性を確認し、必要な表示を追加してください。
